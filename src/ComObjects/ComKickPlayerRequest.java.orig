package ComObjects;

/** 
 * Diese Klasse ist ein spezielles Kommunikations-Objekt.
 * Sie ist eine Nachricht an den Server, die angibt einen
 * Spieler vom Spiel zu entfernen. Dazu enth√§lt es einen String,
 * der den Namen des Spielers enth√§lt.
 */
public class ComKickPlayerRequest extends ComObject {

    /**
	 * Dies ist der Name des Players, der gekicked werden soll.
	 */
	private String playerName;


    /**
<<<<<<< HEAD
     * Dies ist der Kontruktor f¸r eine neue ComKickPlayerRequest-Nachricht.
     * Diese enth‰lt den Namen des Spielers, der aus den Spiel gelˆscht werden soll.
=======
     * Dies ist der Kontruktor f√ºr eine neue ComKickPlayerRequest-Nachricht.
     * Diese enth√§lt den Namen des Spielers, der aus den Spiel gel√∂scht werden soll.
>>>>>>> ViewKlassen
     * @param playerName ist der Name des Spielers.
     */
    public ComKickPlayerRequest(String playerName) {
        this.playerName = playerName;
    }

    /**
     * Diese Methode liefert den Namen des Spielers, der aus dem Spiel
     * entfernt werden soll.
     * @return den Spielernamen.
     */
    public String getPlayerName() {
        return playerName;
    }
}